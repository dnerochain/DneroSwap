export enum WDneroLockStatus {
  NotLocked = 'NotLocked',
  Locking = 'Locking',
  Expired = 'Expired',
  Migrate = 'Migrate',
}

export enum WDneroPoolType {
  NOT_MIGRATED = 0,
  MIGRATED = 1,
  DELEGATED = 2,
}
